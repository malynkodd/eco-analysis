services:

  postgres:
    image: postgres:15
    container_name: eco_postgres
    env_file:
      - .env
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - eco_network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-ecouser} -d ${POSTGRES_DB:-ecodb}"]
      interval: 5s
      timeout: 5s
      retries: 10

  migrations:
    build:
      context: .
      dockerfile: db/Dockerfile.migrations
    container_name: eco_migrations
    env_file:
      - .env
    environment:
      DATABASE_URL: ${DATABASE_URL}
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - eco_network
    restart: "no"

  auth-service:
    build:
      context: .
      dockerfile: services/auth-service/Dockerfile
    container_name: eco_auth
    env_file:
      - .env
    environment:
      ENVIRONMENT:           ${ENVIRONMENT}
      WORKERS:               ${WORKERS}
      DATABASE_URL:          ${DATABASE_URL}
      JWT_PRIVATE_KEY_PATH:  ${JWT_PRIVATE_KEY_PATH}
      JWT_PUBLIC_KEY_PATH:   ${JWT_PUBLIC_KEY_PATH}
      JWT_ISSUER:            ${JWT_ISSUER}
      JWT_AUDIENCE:          ${JWT_AUDIENCE}
      JWT_EXPIRE_MINUTES:    ${JWT_EXPIRE_MINUTES}
      ADMIN_USERNAME:        ${ADMIN_USERNAME}
      ADMIN_PASSWORD:        ${ADMIN_PASSWORD}
      ADMIN_EMAIL:           ${ADMIN_EMAIL}
      CORS_ALLOWED_ORIGINS:  ${CORS_ALLOWED_ORIGINS}
    volumes:
      - ./keys:/run/keys:ro
    depends_on:
      postgres:
        condition: service_healthy
      migrations:
        condition: service_completed_successfully
    networks:
      - eco_network

  project-service:
    build:
      context: .
      dockerfile: services/project-service/Dockerfile
    container_name: eco_project
    env_file:
      - .env
    environment:
      ENVIRONMENT:          ${ENVIRONMENT}
      WORKERS:              ${WORKERS}
      DATABASE_URL:         ${DATABASE_URL}
      JWT_PUBLIC_KEY_PATH:  ${JWT_PUBLIC_KEY_PATH}
      JWT_ISSUER:           ${JWT_ISSUER}
      JWT_AUDIENCE:         ${JWT_AUDIENCE}
      CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS}
    volumes:
      - ./keys/jwt_public.pem:/run/keys/jwt_public.pem:ro
    depends_on:
      postgres:
        condition: service_healthy
      migrations:
        condition: service_completed_successfully
    networks:
      - eco_network

  financial-service:
    build:
      context: .
      dockerfile: services/financial-service/Dockerfile
    container_name: eco_financial
    env_file:
      - .env
    environment:
      ENVIRONMENT:          ${ENVIRONMENT}
      WORKERS:              ${WORKERS}
      DATABASE_URL:         ${DATABASE_URL}
      JWT_PUBLIC_KEY_PATH:  ${JWT_PUBLIC_KEY_PATH}
      JWT_ISSUER:           ${JWT_ISSUER}
      JWT_AUDIENCE:         ${JWT_AUDIENCE}
      CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS}
    volumes:
      - ./keys/jwt_public.pem:/run/keys/jwt_public.pem:ro
    depends_on:
      postgres:
        condition: service_healthy
      migrations:
        condition: service_completed_successfully
    networks:
      - eco_network

  eco-impact-service:
    build:
      context: .
      dockerfile: services/eco-impact-service/Dockerfile
    container_name: eco_impact
    env_file:
      - .env
    environment:
      ENVIRONMENT:          ${ENVIRONMENT}
      WORKERS:              ${WORKERS}
      DATABASE_URL:         ${DATABASE_URL}
      JWT_PUBLIC_KEY_PATH:  ${JWT_PUBLIC_KEY_PATH}
      JWT_ISSUER:           ${JWT_ISSUER}
      JWT_AUDIENCE:         ${JWT_AUDIENCE}
      CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS}
    volumes:
      - ./keys/jwt_public.pem:/run/keys/jwt_public.pem:ro
    depends_on:
      postgres:
        condition: service_healthy
      migrations:
        condition: service_completed_successfully
    networks:
      - eco_network

  multi-criteria-service:
    build:
      context: .
      dockerfile: services/multi-criteria-service/Dockerfile
    container_name: eco_multicriteria
    env_file:
      - .env
    environment:
      ENVIRONMENT:          ${ENVIRONMENT}
      WORKERS:              ${WORKERS}
      DATABASE_URL:         ${DATABASE_URL}
      JWT_PUBLIC_KEY_PATH:  ${JWT_PUBLIC_KEY_PATH}
      JWT_ISSUER:           ${JWT_ISSUER}
      JWT_AUDIENCE:         ${JWT_AUDIENCE}
      CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS}
    volumes:
      - ./keys/jwt_public.pem:/run/keys/jwt_public.pem:ro
    depends_on:
      postgres:
        condition: service_healthy
      migrations:
        condition: service_completed_successfully
    networks:
      - eco_network

  scenario-service:
    build:
      context: .
      dockerfile: services/scenario-service/Dockerfile
    container_name: eco_scenario
    env_file:
      - .env
    environment:
      ENVIRONMENT:          ${ENVIRONMENT}
      WORKERS:              ${WORKERS}
      DATABASE_URL:         ${DATABASE_URL}
      JWT_PUBLIC_KEY_PATH:  ${JWT_PUBLIC_KEY_PATH}
      JWT_ISSUER:           ${JWT_ISSUER}
      JWT_AUDIENCE:         ${JWT_AUDIENCE}
      CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS}
    volumes:
      - ./keys/jwt_public.pem:/run/keys/jwt_public.pem:ro
    depends_on:
      postgres:
        condition: service_healthy
      migrations:
        condition: service_completed_successfully
    networks:
      - eco_network

  comparison-service:
    build:
      context: .
      dockerfile: services/comparison-service/Dockerfile
    container_name: eco_comparison
    env_file:
      - .env
    environment:
      ENVIRONMENT:          ${ENVIRONMENT}
      WORKERS:              ${WORKERS}
      DATABASE_URL:         ${DATABASE_URL}
      JWT_PUBLIC_KEY_PATH:  ${JWT_PUBLIC_KEY_PATH}
      JWT_ISSUER:           ${JWT_ISSUER}
      JWT_AUDIENCE:         ${JWT_AUDIENCE}
      CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS}
    volumes:
      - ./keys/jwt_public.pem:/run/keys/jwt_public.pem:ro
    depends_on:
      postgres:
        condition: service_healthy
      migrations:
        condition: service_completed_successfully
    networks:
      - eco_network

  report-service:
    build:
      context: .
      dockerfile: services/report-service/Dockerfile
    container_name: eco_report
    env_file:
      - .env
    environment:
      ENVIRONMENT:          ${ENVIRONMENT}
      WORKERS:              ${WORKERS}
      JWT_PUBLIC_KEY_PATH:  ${JWT_PUBLIC_KEY_PATH}
      JWT_ISSUER:           ${JWT_ISSUER}
      JWT_AUDIENCE:         ${JWT_AUDIENCE}
      CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS}
    volumes:
      - ./keys/jwt_public.pem:/run/keys/jwt_public.pem:ro
    networks:
      - eco_network

  frontend:
    build: ./frontend
    container_name: eco_frontend
    networks:
      - eco_network

  nginx:
    image: nginx:alpine
    container_name: eco_nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/errors:/usr/share/nginx/html/errors:ro
      - ./nginx/ssl:/etc/nginx/ssl:ro
    depends_on:
      - auth-service
      - project-service
      - financial-service
      - eco-impact-service
      - multi-criteria-service
      - scenario-service
      - comparison-service
      - report-service
      - frontend
    networks:
      - eco_network

volumes:
  postgres_data:

networks:
  eco_network:
    driver: bridge
